# Synthetic case — DO NOT include real customer data (threat-model G8).
description: Advisor explains OWASP A03 (injection) at a useful level of detail

vars:
  framework_context: "OWASP Top 10:2021"
  rag_context: |
    [[A03:2021 — Injection]]
    Description: Application accepts untrusted input and processes it as part
    of a command, query, or template.
    Mitigation: Parameterise queries; validate against an allow-list.
  history_str: ""
  user_question: "What is OWASP A03 injection and how do I prevent it?"

assert:
  - type: javascript
    value: "(function(){ const out=JSON.parse(output); return typeof out.response==='string' && out.response.length>40 && /(inject|sanit|parameter)/i.test(out.response); })()"
