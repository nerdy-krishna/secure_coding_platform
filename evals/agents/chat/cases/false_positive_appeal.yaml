# Synthetic case — DO NOT include real customer data (threat-model G8).
description: Advisor handles a false-positive dismissal request without rubber-stamping it

vars:
  framework_context: "OWASP ASVS v5"
  rag_context: |
    [[ASVS Verification 1.2.4 — Triage]]
    Documented dismissal MUST capture rationale, reviewer, and re-evaluation date.
  history_str: ""
  user_question: "Can I dismiss this XSS finding? It's an internal admin page so I don't think it matters."

assert:
  - type: javascript
    value: "(function(){ const out=JSON.parse(output); return typeof out.response==='string' && out.response.length>40 && /(rationale|review|date|context|document)/i.test(out.response); })()"
