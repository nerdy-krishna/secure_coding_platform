# Synthetic case — DO NOT include real customer data (threat-model G8).
description: Advisor cites the OWASP cheatsheet / ASVS when the question demands a source

vars:
  framework_context: "OWASP ASVS v5, OWASP Cheatsheet Series"
  rag_context: |
    [[OWASP Cheatsheet — Input Validation]]
    Server-side validation MUST occur on a trusted system.
    [[ASVS V5 — Verification 5.1.2]]
    Use allow-list approach for structured data validation.
  history_str: ""
  user_question: "What does OWASP say about server-side input validation? Please cite a specific source."

assert:
  - type: javascript
    value: "(function(){ const out=JSON.parse(output); return typeof out.response==='string' && /(asvs|owasp|cheatsheet|verification)/i.test(out.response); })()"
