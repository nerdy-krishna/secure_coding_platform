# Shared Promptfoo config fragment.
# Referenced by per-agent configs via YAML anchors / explicit copy.
# Exists today as a documentation artifact — the per-agent configs
# inline what they need so each one is self-contained for promptfoo's
# loader.

providers:
  - id: file://providers/mock.js
    label: mock

defaultTest:
  options:
    cache: true
